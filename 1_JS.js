// Завдання 1
// Напишіть однорядкове рішення, яке вирахує суму квадратних корнів для всіх парних 
// чисел массиву: [1, 4, 3, 0, 4, 5, 4] має повернути 6

const arr = [1, 4, 3, 0, 4, 5, 4].filter(elem => elem % 2 === 0).reduce((prev, curr) => prev + Math.sqrt(curr), 0);
console.log(arr);